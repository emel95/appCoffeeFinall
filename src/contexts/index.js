import {createContext} from 'react';

const CoffeeContext =createContext({
    // ********در اینجا مقدار نمیدم فقط صرفا تعریف میکنم و مقدار اولیه میدم یعنی فرضا ارایه خالی ابجکت خالی و غیره********
courses:[],
//loading:true,
});
export default CoffeeContext;